import { type Config } from 'prettier';

const config: Config = {
  endOfLine: 'lf',
  semi: true,
  useTabs: false,
  singleQuote: true,
  arrowParens: 'avoid',
  tabWidth: 2,
  printWidth: 80,
  trailingComma: 'es5',
  plugins: [],
};

export default config;
